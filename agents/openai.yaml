interface:
  display_name: "Qingshan Medical Map Update"
  short_description: "Staged medical map CSV update wizard"
  default_prompt: "Use $qingshan-medical-map-data-update-skill to start at Stage 0 and guide me through the medical map update workflow with staged summaries, safety checks, and explicit handoff prompts."

policy:
  allow_implicit_invocation: true
